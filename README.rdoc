== MongoBrowser


This is proof-of-concept rails website for navigating through MongoDB databases with ease, avoiding the console.

As of now, it will only connect to databases on <tt>localhost:27017</tt>. 

To Do: remove ActiveRecord, properly implement MongoDB
